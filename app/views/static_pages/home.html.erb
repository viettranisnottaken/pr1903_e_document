<h1 style="text-align: center">Welcome to E-doc hehe</h1>

<h2>Available documents</h2>

<div class="row">
  <% Document.all.each do |d| %>
    <div class="col-2">
      <div class="card">
        <%= image_tag d.file_name.preview(size: "50x50"), class: "card-img-top" %>
        <div class="card-body">
          <p>Description: <%= d.description %></p>
          <p>Author: <%= d.author %></p>
          <p><%= d.view_count %> views</p>
          <p><%= d.download_count %> downloads</p>
          <p><%= link_to "document show", d %></p>
          <p><%= link_to "Download this document", download_pdf_document_path(d), target: "blank" %></p>
        </div>
      </div>
    </div>
  <% end %>
</div>

<% if logged_in? %>
  <hr>

  <h2>Lastest documents</h2>
  <div class="row">
    <% @latest_documents.each do |d| %>
      <div class="col-2">
        <div class="card">
          <%= image_tag d.file_name.preview(size: "50x50"), class: "card-img-top" %>
          <div class="card-body">
            <p>Description: <%= d.description %></p>
            <p>Author: <%= d.author %></p>
            <p><%= d.view_count %> views</p>
            <p><%= d.download_count %> downloads</p>
            <p><%= link_to "document show", d %></p>
            <p><%= link_to "Download this document", download_pdf_document_path(d), target: "blank" %></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <hr>

  <h2>Most viewed documents</h2>
  <div class="row">
    <% @most_viewed_documents.each do |d| %>
      <div class="col-2">
        <div class="card">
          <%= image_tag d.file_name.preview(size: "50x50"), class: "card-img-top" %>
          <div class="card-body">
            <p>Description: <%= d.description %></p>
            <p>Author: <%= d.author %></p>
            <p><%= d.view_count %> views</p>
            <p><%= d.download_count %> downloads</p>
            <p><%= link_to "document show", d %></p>
            <p><%= link_to "Download this document", download_pdf_document_path(d), target: "blank" %></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
<% end %>
